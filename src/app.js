"use strict";
const add = (a, b) => {
    console.log(`Adding ${a} and ${b}`);
    console.log(`The sum of ${a} and ${b} is being calculated.`);
    return a + b;
};
console.log(add(2, 3)); // Output: 8
